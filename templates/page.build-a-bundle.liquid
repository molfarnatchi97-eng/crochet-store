<div class="bundle-container">
  <div class="container">
    <div class="bundle-intro">
      <h1>{{ page.title }}</h1>
      {% if page.content != blank %}
        <div class="rte">
          {{ page.content }}
        </div>
      {% else %}
        <p>Curate your own ultimate growth library! Choose exactly 6 of our premium self-improvement and mental productivity guides below, and get them all bundled together for one low, fixed price. Your digital PDF guide downloads will be delivered instantly after checkout.</p>
      {% endif %}
    </div>

    {% render 'build-a-bundle' %}
  </div>
</div>
